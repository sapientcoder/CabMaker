namespace CabMaker
{
    /// <summary>
    /// Identifies the compression engine/type.
    /// </summary>
    public enum CompressionType
    {
        None,
        MSZIP,
        LZX
    }
    public enum FileExtension
    {
        CAB,
        WSP,
        XSN

    }
}
